# FieldOps Backend

FastAPI service, simulation engine, agents, and evaluation utilities for the FieldOps demo.

## Run

```bash
pip install -e .[dev]
uvicorn app.main:app --reload
```

## Tests

```bash
pytest
```

