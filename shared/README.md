# Shared Contracts

`incident.schema.json` captures the API-level incident shape shared between backend and frontend. The backend implements these contracts with Pydantic models and the frontend mirrors the relevant types in TypeScript.

